<html>
<head>
<title>Bills</title>
</head>
<body>
    <nav>
        <a id="login" href="{{ url_for('login') }}">Login</a>
        <a id="bills" href="{{ url_for('all') }}">Bills</a>
        <a id="users" href="{{ url_for('users') }}">Users</a>
        <a id="createuser" href="{{ url_for('createuser') }}">Add new user</a>
        <a id="createbill" href="{{ url_for('createbill') }}">Add new bill</a>
        <a id="paidby" href="{{ url_for('paidby') }}">Add payer to bill</a>
        <a id="report" href="{{ url_for('report') }}">Total all bills</a>
    </nav>
	<h1>Bill Splitter</h1>
	<h2>{% block subtitle %} {% endblock subtitle %}</h2>
	{% block content %} {% endblock content %}
</body>
</html>
